import { memo } from 'react';

/**
 * Previously rendered three fabricated customer testimonials attributed
 * to invented people at invented companies (Northwind, Lattice, Helios)
 * with made-up "result" metrics. Removed 2026-06-09 — do not reintroduce
 * fictional quotes. Replace only with verifiable customer quotes that have
 * explicit permission to publish (name, role, company, and the metric must
 * all be real and sourced).
 */
export const B2BTestimonials = memo(function B2BTestimonials() {
  return null;
});

B2BTestimonials.displayName = 'B2BTestimonials';
