# ──────────────────────────────────────────────────────────────────────────
# Breakout pipeline — open-source dependencies (v2 / VFX-enhanced).
# Tested on Python 3.10–3.12 + CUDA 12.x.
#
# Optional binaries (install separately if you want their speed):
#   • rife-ncnn-vulkan          — frame interpolation
#   • realesrgan-ncnn-vulkan    — upscaling
#   • ffmpeg                    — encoding + audio mix
# ──────────────────────────────────────────────────────────────────────────

# Core ML
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0
transformers>=4.46.0
diffusers>=0.32.0
accelerate>=1.0.0
safetensors>=0.4.3
sentencepiece>=0.2.0

# Quantisation (4-bit bitsandbytes path)
bitsandbytes>=0.43.0

# Vision / compositing
opencv-python>=4.10.0.84
numpy>=1.26.0
scipy>=1.13.0
Pillow>=10.3.0

# Video assembly
moviepy>=1.0.3
ffmpeg-python>=0.2.0
imageio>=2.34.0
imageio-ffmpeg>=0.4.9

# Misc utilities
einops>=0.7.0
protobuf>=4.25.3

# ── Optional (recommended for full quality) ──
# Real-ESRGAN PyTorch (if not using the ncnn binary)
# realesrgan>=0.3.0
# basicsr>=1.4.2

# Frame interpolation via FILM (tensorflow path)
# tensorflow>=2.15.0
# tensorflow-hub>=0.16.0

# AudioLDM2 / MusicGen for fully generative audio
# audiocraft>=1.3.0
# scipy>=1.13.0           # AudioLDM2 wav export

# Depth-Anything-V2 (via transformers, already installed above)
# — uses the transformers.pipeline("depth-estimation") path
